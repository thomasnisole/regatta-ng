import { BoatNumberValidatorDirective } from './boat-number-validator.directive';

describe('BoatNumberValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new BoatNumberValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
