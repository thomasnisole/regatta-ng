import { CheckLineDirective } from './check-line.directive';

describe('CheckLineDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckLineDirective();
    expect(directive).toBeTruthy();
  });
});
