import { BuoyDirective } from './buoy.directive';

describe('BuoyDirective', () => {
  it('should create an instance', () => {
    const directive = new BuoyDirective();
    expect(directive).toBeTruthy();
  });
});
